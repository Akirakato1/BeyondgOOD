package edu.cs3500.spreadsheets.provider.model;

/**
 * Provider's SingleRef. We need this so our code compiles,
 * but it doesn't matter how the provider implemented this as we don't use it
 * in our adapter.
 */
public class SingleRef {

}
